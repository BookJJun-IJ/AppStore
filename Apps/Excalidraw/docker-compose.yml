name: excalidraw

services:
  # Redis - 데이터 저장소
  redis:
    image: redis:7-alpine
    container_name: redis
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.1'
    volumes:
      - /DATA/AppData/$AppID/redis:/data
    command: redis-server --appendonly yes

  # Storage Backend - 그림 저장 및 공유 서버
  storage:
    image: kiliandeca/excalidraw-storage-backend:latest
    platform: linux/amd64
    container_name: storage
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    environment:
      - PORT=8081
      - STORAGE_URI=redis://redis:6379
      - LOG_LEVEL=info
      - GLOBAL_PREFIX=/api/v2
    expose:
      - 8081
    depends_on:
      - redis
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8081/api/v2/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # Room Server - 실시간 협업 서버
  room:
    image: excalidraw/excalidraw-room:sha-49bf529
    platform: linux/amd64
    container_name: room
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.1'
    expose:
      - 80

  # Excalidraw Frontend - 메인 애플리케이션
  excalidraw:
    image: alswl/excalidraw:v0.17.3-fork-b1
    platform: linux/amd64
    user: "0:0"
    container_name: excalidraw
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    expose:
      - 80
    environment:
      - TZ=$TZ
      - DOMAIN=$domain
      # Storage Backend 연결 설정
      - VITE_APP_BACKEND_V2_GET_URL=http://storage:8081/api/v2/scenes/
      - VITE_APP_BACKEND_V2_POST_URL=http://storage:8081/api/v2/scenes/
      - VITE_APP_HTTP_STORAGE_BACKEND_URL=http://storage:8081/api/v2
      - VITE_APP_STORAGE_BACKEND=http
      # Room Server 연결 설정  
      - VITE_APP_WS_SERVER_URL=ws://room:80
      # Firebase 비활성화
      - VITE_APP_FIREBASE_CONFIG={}
    depends_on:
      - storage
      - room
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:80/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 60s

x-casaos:
  architectures:
    - amd64
    - arm64
  main: excalidraw
  webui_port: 80
  author: CasaOS Team
  category: Productivity
  description:
    en_us: Excalidraw is a virtual collaborative whiteboard tool with real-time collaboration, server-side storage, and sharing capabilities.
    ko_kr: 실시간 협업, 서버 저장, 공유 기능을 갖춘 가상 협업 화이트보드 도구입니다.
    zh_cn: Excalidraw 是一个具有实时协作、服务器端存储和共享功能的虚拟协作白板工具。
    fr_fr: Excalidraw est un outil de tableau blanc collaboratif virtuel avec collaboration en temps réel et stockage côté serveur.
    es_es: Excalidraw es una herramienta de pizarra colaborativa virtual con colaboración en tiempo real y almacenamiento del lado del servidor.
  developer: Excalidraw Team
  icon: https://cdn.jsdelivr.net/gh/Yundera/AppStore@main/Apps/Excalidraw/icon.png
  screenshot: https://cdn.jsdelivr.net/gh/Yundera/AppStore@main/Apps/Excalidraw/screenshot-1.png
  tagline:
    en_us: Virtual collaborative whiteboard with server storage
    ko_kr: 서버 저장 기능을 갖춘 가상 협업 화이트보드
    zh_cn: 具有服务器存储的虚拟协作白板
    fr_fr: Tableau blanc collaboratif virtuel avec stockage serveur
    es_es: Pizarra colaborativa virtual con almacenamiento en servidor
  thumbnail: https://cdn.jsdelivr.net/gh/Yundera/AppStore@main/Apps/Excalidraw/thumbnail.png
  title:
    en_us: Excalidraw
  tips:
    before_install:
      en_us: |
        **🚀 Stable Excalidraw (Health Check Fixed):**
        
        **✅ Real-time Collaboration**
        - Multiple users can edit simultaneously
        - Live cursor tracking and updates
        
        **✅ Server Storage**
        - All drawings saved to Redis database
        - Persistent storage across browser sessions
        - Shareable links functionality
        
        **✅ Stability Improvements**
        - Fixed health check timeouts
        - Improved container startup sequence
        - More reliable service dependencies
        
        **🔧 Total Resources:**
        - Memory: ~768MB (Redis: 128MB + Storage: 256MB + Room: 128MB + App: 256MB)
        - CPU: ~0.7 cores
        
        **📝 Note:**
        This version focuses on stability and core functionality.
        Advanced file export features can be added later once the base system is working.
      ko_kr: |
        **🚀 안정적인 Excalidraw (헬스체크 수정됨):**
        
        **✅ 실시간 협업**
        - 여러 사용자가 동시에 편집
        - 실시간 커서 추적 및 업데이트
        
        **✅ 서버 저장**
        - 모든 그림이 Redis 데이터베이스에 저장
        - 브라우저 세션 간 지속적 저장
        - 공유 링크 기능
        
        **✅ 안정성 개선**
        - 헬스체크 타임아웃 수정
        - 컨테이너 시작 순서 개선
        - 더 안정적인 서비스 의존성
        
        **🔧 총 리소스:**
        - 메모리: ~768MB (Redis: 128MB + Storage: 256MB + Room: 128MB + App: 256MB)
        - CPU: ~0.7 코어
        
        **📝 참고:**
        이 버전은 안정성과 핵심 기능에 중점을 둡니다.
        고급 파일 내보내기 기능은 기본 시스템이 작동한 후 나중에 추가할 수 있습니다.
  index: /