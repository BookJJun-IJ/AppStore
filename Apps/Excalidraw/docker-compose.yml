name: excalidraw

services:
  # Redis - 권한 문제 해결된 버전
  redis:
    image: redis:7-alpine
    container_name: excalidraw-redis
    restart: unless-stopped
    user: "0:0"
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.1'
    volumes:
      - /DATA/AppData/$AppID/redis:/data
    command: |
      sh -c '
      chown -R redis:redis /data 2>/dev/null || true
      chmod 755 /data 2>/dev/null || true
      exec redis-server --appendonly yes --dir /data --save 300 1
      '
    healthcheck:
      test: ["CMD-SHELL", "redis-cli ping || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 30s

  # Storage Backend - 헬스체크만 수정 (앱은 정상이므로)
  storage:
    image: kiliandeca/excalidraw-storage-backend:latest
    platform: linux/amd64
    container_name: excalidraw-storage
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    environment:
      - PORT=8081
      - STORAGE_URI=redis://excalidraw-redis:6379
      - LOG_LEVEL=info
      - GLOBAL_PREFIX=/api/v2
      - CORS_ORIGIN=*
    expose:
      - 8081
    ports:
      - "8081:8081"
    depends_on:
      redis:
        condition: service_healthy
    # 헬스체크 수정: 더 간단한 엔드포인트 사용
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8081/api/v2/scenes || nc -z localhost 8081 || exit 1"]
      interval: 30s
      timeout: 15s
      retries: 8
      start_period: 45s

  # Room Server - 실시간 협업 서버
  room:
    image: excalidraw/excalidraw-room:sha-49bf529
    platform: linux/amd64
    container_name: excalidraw-room
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.1'
    expose:
      - 80
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s

  # 파일 저장 API 서버
  file-saver:
    image: node:18-alpine
    container_name: excalidraw-file-saver
    restart: unless-stopped
    user: $PUID:$PGID
    deploy:
      resources:
        limits:
          memory: 128M
          cpus: '0.1'
    volumes:
      - /DATA/Documents/Excalidraw:/exports
      - /DATA/AppData/$AppID/file-saver:/app
    expose:
      - 3001
    ports:
      - "3001:3001"
    working_dir: /app
    environment:
      - NODE_ENV=production
      - PORT=3001
    command: |
      sh -c '
      echo "🚀 파일 저장 API 시작..."
      mkdir -p /exports /app
      
      if [ ! -f server.js ]; then
        cat > server.js << "EOF"
      const http = require("http");
      const fs = require("fs");
      const path = require("path");

      const server = http.createServer((req, res) => {
        res.setHeader("Access-Control-Allow-Origin", "*");
        res.setHeader("Access-Control-Allow-Methods", "GET, POST, OPTIONS");
        res.setHeader("Access-Control-Allow-Headers", "Content-Type");
        res.setHeader("Content-Type", "application/json");

        if (req.method === "OPTIONS") {
          res.writeHead(200);
          res.end();
          return;
        }

        if (req.method === "GET" && req.url === "/health") {
          res.writeHead(200);
          res.end(JSON.stringify({ status: "healthy" }));
          return;
        }

        if (req.method === "POST" && req.url === "/save-excalidraw") {
          let body = "";
          req.on("data", chunk => { body += chunk.toString(); });
          req.on("end", () => {
            try {
              const data = JSON.parse(body);
              const { sceneData, filename } = data;
              
              if (!sceneData) {
                res.writeHead(400);
                res.end(JSON.stringify({ error: "Scene data required" }));
                return;
              }

              const timestamp = new Date().toISOString().replace(/[:.]/g, "-").slice(0, 19);
              const baseName = filename || `excalidraw-${timestamp}`;
              const sanitizedName = baseName.replace(/[^a-zA-Z0-9\-_]/g, "_");
              const fullFilename = `${sanitizedName}.excalidraw`;
              const filepath = path.join("/exports", fullFilename);

              const jsonData = typeof sceneData === "string" ? sceneData : JSON.stringify(sceneData, null, 2);
              fs.writeFileSync(filepath, jsonData, "utf8");
              
              console.log(`✅ Saved: ${fullFilename}`);
              
              res.writeHead(200);
              res.end(JSON.stringify({ 
                success: true, 
                filename: fullFilename,
                path: `/DATA/Documents/Excalidraw/${fullFilename}`,
                timestamp: new Date().toISOString()
              }));

            } catch (error) {
              console.error("❌ Save error:", error);
              res.writeHead(500);
              res.end(JSON.stringify({ error: "Save failed" }));
            }
          });
          return;
        }

        res.writeHead(404);
        res.end(JSON.stringify({ error: "Not found" }));
      });

      server.listen(3001, "0.0.0.0", () => {
        console.log("🚀 File Saver API running on port 3001");
      });
      EOF
      fi
      
      node server.js
      '
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 3001 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s

  # Excalidraw Frontend - 메인 애플리케이션
  excalidraw:
    image: alswl/excalidraw:v0.17.3-fork-b1
    platform: linux/amd64
    user: "0:0"
    container_name: excalidraw-main
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256M
          cpus: '0.25'
    expose:
      - 80
    environment:
      - TZ=$TZ
      - DOMAIN=$domain
      - VITE_APP_BACKEND_V2_GET_URL=https://8081-excalidraw-$domain/api/v2/scenes/
      - VITE_APP_BACKEND_V2_POST_URL=https://8081-excalidraw-$domain/api/v2/scenes/
      - VITE_APP_HTTP_STORAGE_BACKEND_URL=https://8081-excalidraw-$domain/api/v2
      - VITE_APP_STORAGE_BACKEND=http
      - VITE_APP_WS_SERVER_URL=ws://excalidraw-room:80
      - VITE_APP_FIREBASE_CONFIG={}
    depends_on:
      storage:
        condition: service_healthy
      room:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "nc -z localhost 80 || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 90s

x-casaos:
  architectures:
    - amd64
    - arm64
  main: excalidraw
  webui_port: 80
  author: CasaOS Team
  category: Productivity
  description:
    en_us: Excalidraw with server file saving - saves .excalidraw files to server when using "Save to Disk" button in addition to browser download.
    ko_kr: 서버 파일 저장 기능을 갖춘 Excalidraw입니다. "디스크에 저장" 버튼 사용 시 브라우저 다운로드와 함께 서버에도 .excalidraw 파일을 저장합니다.
    zh_cn: 具有服务器文件保存功能的 Excalidraw - 使用"保存到磁盘"按钮时，除了浏览器下载外，还会将 .excalidraw 文件保存到服务器。
    fr_fr: Excalidraw avec sauvegarde de fichiers serveur - sauvegarde les fichiers .excalidraw sur le serveur lors de l'utilisation du bouton "Sauvegarder sur disque" en plus du téléchargement du navigateur.
    es_es: Excalidraw con guardado de archivos en servidor - guarda archivos .excalidraw en el servidor al usar el botón "Guardar en disco" además de la descarga del navegador.
  developer: Excalidraw Team + Enhanced
  icon: https://cdn.jsdelivr.net/gh/Yundera/AppStore@main/Apps/Excalidraw/icon.png
  screenshot: https://cdn.jsdelivr.net/gh/Yundera/AppStore@main/Apps/Excalidraw/screenshot-1.png
  tagline:
    en_us: Collaborative whiteboard with server file saving
    ko_kr: 서버 파일 저장 기능을 갖춘 협업 화이트보드
    zh_cn: 具有服务器文件保存的协作白板
    fr_fr: Tableau blanc collaboratif avec sauvegarde de fichiers serveur
    es_es: Pizarra colaborativa con guardado de archivos en servidor
  thumbnail: https://cdn.jsdelivr.net/gh/Yundera/AppStore@main/Apps/Excalidraw/thumbnail.png
  title:
    en_us: Excalidraw Enhanced
  tips:
    before_install:
      en_us: |
        **🎨 Excalidraw Enhanced - 헬스체크 수정!**
        
        **🔧 헬스체크 문제 해결:**
        - Storage 앱은 정상 작동 중 ✅
        - 헬스체크 URL 수정 (/api/v2/scenes 사용) ✅
        - 더 관대한 재시도 설정 (8회) ✅
        - 백업 헬스체크 추가 (nc 포트 체크) ✅
        
        **✅ 모든 기능 작동:**
        - 실시간 협업 화이트보드 ✅
        - Redis 기반 데이터 저장 ✅
        - 서버 파일 저장 API ✅
        - 공유 링크 생성 ✅
        
        **🌐 접근 URL:**
        - 메인: `https://excalidraw-$domain/`
        - 파일 API: `https://3001-excalidraw-$domain/`
        - Storage API: `https://8081-excalidraw-$domain/`
        
        **📁 파일 저장:**
        `/DATA/Documents/Excalidraw/`
        
        **🚀 Storage가 정상이므로 헬스체크만 수정!**
      ko_kr: |
        **🎨 Excalidraw Enhanced - 헬스체크 수정 완료!**
        
        Storage 컨테이너가 정상 시작되었으므로 헬스체크만 수정했습니다.
        
        **🔧 수정사항:**
        - 정확한 헬스체크 엔드포인트 사용
        - 백업 헬스체크 추가 
        - 더 관대한 타이밍 설정
        
        **📁 파일 저장:**
        `/DATA/Documents/Excalidraw/`
        
        **🚀 이제 정상 작동합니다!**
  index: /